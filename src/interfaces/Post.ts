export interface Post {
  id: string;        
  userId: string;    
  avatarUrl: string;  
  location: string;  
  imageUrl: string;  
  caption: string;    
  likesCount: number; 
  isLiked: boolean; 
}